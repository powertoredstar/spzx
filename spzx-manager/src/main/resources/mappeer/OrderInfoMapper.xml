<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace等于mapper接口类的全限定名,这样实现对应 -->
<mapper namespace="xiaoxin.spzx.manager.mapper.OrderInfoMapper">


    <select id="selectOrderStatistic" resultType="xiaoxin.spzx.model.entity.order.OrderStatistics">
        select date_format(oi.create_time,'%Y-%m-%d') orderDate,sum(oi.total_amount) totalAmount ,count(oi.id) totalNum
        from order_info oi
        where date_format(oi.create_time,'%Y-%m-%d') =#{createTime}
        GROUP BY date_format(oi.create_time,'%Y-%m-%d')
    </select>
</mapper>